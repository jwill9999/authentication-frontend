# Dependencies — reinstalled inside the container via npm ci
node_modules

# Build output — produced inside the builder stage
dist
.vite
*.tsbuildinfo

# Test coverage reports
coverage

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Git internals
.git
.gitignore
.husky

# Editor config
.vscode
*.local

# OS files
.DS_Store

# Environment files — passed as build args or runtime env vars instead
.env
.env.*
!.env.example

# Docs — not needed in the image
docs

# Test files — not needed in the production image
src/**/*.test.ts
src/**/*.test.tsx
src/test
