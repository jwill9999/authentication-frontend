# ── Frontend ──────────────────────────────────────────────────────────────────
# IMPORTANT: Values in frontend env files are not secret.
# Do NOT place passwords, private keys, JWT secrets, or database credentials here.
# API base URL — passed as a Docker build arg (baked in by Vite at build time).
# To proxy through nginx (no CORS issues): set to /api and uncomment the
# nginx proxy block in nginx.conf.
VITE_API_URL=http://localhost:3000

# Port to expose the frontend container on (default: 80)
FRONTEND_PORT=80
